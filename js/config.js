// Baked-in key: read by data.js for every fetch. Browsers can override via localStorage after a paste in the UI.
// See README for public deploy vs. local `config.local.js`.

export const NASA_API_KEY = "DEMO_KEY";
