// Copy to `js/config.local.js` (gitignored) and set your key, or use root `.env`
// + `node scripts/env-to-local.mjs` to generate that file from NASA_API_KEY.

export const NASA_API_KEY = "PASTE_YOUR_NASA_KEY_HERE";
